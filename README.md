# Phys490 Assignment 4

- name: Matthew Man
- student ID: 20602376

## Dependencies

- numpy
- matplotlib
- torch
- torchvision

## Running `main.py`

To run `main.py`, use

```sh
python3 main.py -n 100
```
-n is number of generated images (default: 100)
-o in results directory (default: result_dir)
-param is parameter json filepath (default: param/param.json)
Other parameters (batch size, number of epochs, learning rate) are input through param.json
